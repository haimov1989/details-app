pipeline {
    agent any
    stages {
        stage('Spellcheck') {
            steps {
                sh 'echo Running spellcheck...'
                // Add spellcheck commands here
            }
        }
    }
}
